import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Preguntados2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
